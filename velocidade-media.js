const distanciaPercorrida = 1000; // metros
const tempo = 800; // segundos

const velocidadeMedia = (distanciaPercorrida / tempo) * 3.6; // multiplicando o resultado da divisão por 3,6, o resultado será em metros por segundo

console.log(`${velocidadeMedia} km/h`);
